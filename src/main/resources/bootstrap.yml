spring:
  application:
    name: openstackgateway
  cloud:
    config:
      name: ${spring.application.name}
      uri: http://configserver:8888, http://localhost:8888 #, http://??:8888, http://?:8888
